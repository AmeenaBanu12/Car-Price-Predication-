# 🚗 Car Price Prediction – Django & Machine Learning Project

This is a Car Price Prediction web application built using **Python, Django, and Machine Learning**.  
The project trains a regression model and uses Django to serve predictions.

---

## 📌 Project Overview

The system predicts car prices based on dataset features using a trained ML model.

---

## 🛠 Technologies Used

- Python  
- Django  
- Pandas  
- Scikit-learn  
- SQLite  

---

## 📁 Files Included

- `train_model.py` – Train ML model  
- `car_price_model.pkl` – Saved trained model  
- `manage.py` – Django project runner  
- `db.sqlite3` – Database  
- `dataset.csv` – Training dataset  

---

## 🚀 How to Run Project

### Step 1: Install packages
```bash
pip install django pandas scikit-learn
# Car-Price-Predication-
